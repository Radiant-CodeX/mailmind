line-length = 120

[lint]
select = ["E", "F", "I"]
ignore = ["E501"]

# Tests and scripts intentionally set env/settings before importing the app.
per-file-ignores = { "scripts/*.py" = ["E402"], "tests/*.py" = ["E402"] }